/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["img.freepik.com" , "images.remotePatterns" , "www.imexpharm.com", "www.google.com" , "localhost"],
  },
   devIndicators: false
};

export default nextConfig;
